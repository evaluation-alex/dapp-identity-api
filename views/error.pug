extends layout
block vars
  - var bodyClass = 'error-page'
block content
  .grid-flex-container
    .grid-flex-cell
      - if (errors && errors.page)
        .message.message-error #{errors.page}
      .container Error
